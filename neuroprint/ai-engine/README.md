# AI Engine

This module contains baseline feature extraction and drift prediction primitives.

## Folders

- `featureExtraction/`: converts raw metrics into numeric vectors
- `driftDetection/`: calculates drift distance from baseline vectors
- `prediction/`: maps score to interpretable risk labels
